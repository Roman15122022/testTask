{
  "name": "testTask",
  "version": "1.0.0",
  "description": "Herdsman prototype built with TypeScript, Vite, and PixiJS.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "private": true,
  "dependencies": {
    "pixi.js": "^7.4.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.9",
    "vitest": "^4.1.5"
  }
}
